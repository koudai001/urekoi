・UIを刷新する

・PairsからTinderlikeに結局tinderのuxが一番使いやすい
その大人版ってことで、Elegantに名前を変更する

・ただここで一旦デプロイまではいっておきたい pairライクのブランチを作って

・AWS/ECS/

tinder-likeに大幅に刷新する

まず作成する画面は
/recs

デザイン渡すので画面を設計する

コンポーネントに分ける

その後にapiを少しずつ移行する

GET /recs
GET /recs/profile プロフィール詳細

POST /likes
POST /skips
