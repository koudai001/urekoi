# モバイル対応

## やること
- [ ] 各画面をSP/PCで見比べて、必要な画面から個別にレイアウトを調整する(SP用に別コンポーネントを切るか、responsiveなクラスで済ませるかは画面ごとに判断)
- [ ] SP/PCを両方DOMに置いてCSSで出し分ける場合、`useWsConnection`のようなmount時に副作用(WS接続等)が走るフックは両方から呼ばれて重複しないよう、分岐より上位の共通の場所で1回だけ呼ぶようにする
